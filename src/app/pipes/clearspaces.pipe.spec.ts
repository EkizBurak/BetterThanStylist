import { ClearspacesPipe } from './clearspaces.pipe';

describe('ClearspacesPipe', () => {
  it('create an instance', () => {
    const pipe = new ClearspacesPipe();
    expect(pipe).toBeTruthy();
  });
});
